pipeline {
    agent any  // Run on any available agent (machine)

        stages {
        
        stage('Run Test') {
            steps {
                // Run tests (e.g., using Maven)
                bat 'mvn clean test'
            }
        }

      }
    }

  

